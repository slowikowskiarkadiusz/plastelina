components:
  schemas:
    BWS.DomainModels.Common:
      Blocked:
        description: Blocked
        $id: Blocked
        type: string
        enum:
          - All
          - No
          - Payment