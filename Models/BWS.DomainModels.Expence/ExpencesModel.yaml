channels:
  inbound-expences:
    description: Subscribe description
    publish:
      summary: Summary Subscribe description
      message:
        $ref: "#/components/messages/ExpencesMessage"

  outbound-expences:
    description: Publish description
    subscribe:
      summary: Summary Publish description
      message:
        $ref: "#/components/messages/ExpencesMessage"

components:
  messages:
    ExpencesMessage:
      name: Expences
      title: Expences
      summary: Expences description
      contentType: application/json
      payload:
        $ref: "#/components/schemas/BWS.DomainModels.Expence/ExpencesModel"
  schemas:
    BWS.DomainModels.Expence:
      ExpencesModel:
        $id: ExpencesModel
        type: object
        properties:
          Expences:
            type: array
            items:
              $ref: "#/components/schemas/BWS.DomainModels.Expence/Expence"
  