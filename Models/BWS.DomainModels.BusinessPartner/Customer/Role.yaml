components:
  schemas:
    BWS.DomainModels.BusinessPartner.Customer:
      Role:
        description: Role
        $id: Role
        type: string
        enum:
          - Business
          - Invoice
          - Collection
          - Statement